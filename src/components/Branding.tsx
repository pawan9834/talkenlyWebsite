import Link from "next/link";export default function Logo({ className = "", onClick }: { className?: string; onClick?: () => void }) {return (<Link href="/" onClick={onClick} className={`flex items-center gap-2 group ${className}`}><div className="w-10 h-10 bg-orange-600 rounded-xl flex items-center justify-center transition-transform group-hover:scale-110"><span className="text-white font-bold text-2xl">T</span></div><span className="text-2xl font-bold tracking-tight text-white uppercase italic">Talkenly</span></Link>);}
